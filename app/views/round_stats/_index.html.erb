<table class="table table-striped">
  <thead>
    <tr>
      <th>Nick</th>
      <th>Kils</th>
      <th>Deaths</th>
      <th>% Hits</th>
      <th>% Headshots</th>
    </tr>
  </thead>
  <tbody>
    <tr>
    <td colspan="5"><strong><font color="red">Terrorists</font></strong></td>
    </tr>
    <% @ts.each do |player| %>
    <% reg_player = PermaStat.find_by_setinfo_pass(player.setinfo_pass) %>
    <tr>
      <td><%= if reg_player then link_to(player.name, "users/".concat(reg_player.name), :class => "popover-with-html", :title => reg_player.name, "data-content" => "shit") else player.name end %></td>
      <td><%= player.kills %></td>
      <td><%= player.deaths %></td>
      <td><%= (100 * player.hits.to_f/player.shots).round(2) %></td>
      <td><%= (100 * player.hits.to_f/player.shots).round(2) %></td>
    </tr>
    <% end %>
    <tr>
    <td colspan="5" align="center"><strong><font color="blue">Counter Terrorists</font></strong></td>
    </tr>
    <% @cts.each do |player| %>
    <tr>
      <td align="right"><%= player.name %></td>
      <td align="right"><%= player.kills %></td>
      <td align="right"><%= player.deaths %></td>
      <td align="right"><%= (100 * player.hits.to_f/player.shots).round(2) %></td>
      <td align="right"><%= (100 * player.hits.to_f/player.shots).round(2) %></td>
    </tr>
    <% end %>
  </tbody>
</table>
